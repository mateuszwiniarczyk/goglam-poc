import { Head, Html, Main, NextScript } from 'next/document';

const Document = () => (
  <Html lang='en'>
    <Head />
    <body className='bg-gray-100 antialiased overflow-x-hidden'>
      <Main />
      <NextScript />
    </body>
  </Html>
);

export default Document;
